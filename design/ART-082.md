# Upright spring artwork, v0.8.2

The built-in ImageGen tool edited the spring concept. The generated lower-center cell was extracted to transparency and composited into the approved v1 atlas, preserving the other five cells. Runtime asset: `assets/ui/surprise-items-v2.png`.

Final prompt:

> Use case: precise-object-edit. Asset type: transparent 3-by-2 game item atlas for Adam’s Summit. Edit only the spring in the bottom-center cell. Replace the diagonal spring with an upright vertical launch spring, viewed straight-on, base exactly horizontal, top pad exactly horizontal, coil centered and compressed slightly as if ready to launch upward. Keep it unmistakably useful for vertical climbing. Polished hand-painted storybook game asset with crisp high-resolution edges, teal enamel caps, warm gold coil, matching the existing chest, shield, ink bottle, sticky pod, and cloak. Preserve the exact 768×512 canvas, exact 3 columns by 2 rows, transparent padding and item centers. The replacement spring must remain fully inside the bottom-center 256×256 cell and fill a similar visual size. Change only the bottom-center spring; keep all other five items unchanged; transparent background; no text; no watermark; no tilt; no diagonal orientation.
